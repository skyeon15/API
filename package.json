{
  "name": "pds-monorepo",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "start": "turbo run start:prod --filter=api & turbo run start --filter=web",
    "start:api": "npm run start:prod --workspace=apps/api",
    "start:web": "npm run start --workspace=apps/web",
    "migration:run": "npm run migration:run --workspace=apps/api"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.4.2"
  }
}
